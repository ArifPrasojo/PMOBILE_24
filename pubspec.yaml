name: flutter_application_api
description: "A new Flutter project API."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '3.4.3'

dependencies:
  flutter:
    sdk: flutter
  geolocator: ^13.0.2
  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: 3.0.2

flutter:
  uses-material-design: true

  assets:
    - termometer.jpg
  # - barometer2.jpg
    - humidity.jpg
    - cloudy.jpg
    - baro.jpg